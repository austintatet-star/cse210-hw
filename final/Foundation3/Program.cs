using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("--- PROGRAM 3: INHERITANCE WITH EVENT PLANNING ---");
        Console.WriteLine("[Skeleton application structure - complete implementation required]");
        
        // // Create an EventAddress object for your events
        // EventAddress eventAddress = new EventAddress(attributes);

        // // Create at least one Lecture object, set its values, and call its methods
        // Lecture lectureEvent = new Lecture(attributes);
        // Console.WriteLine(lectureEvent.GetStandardDetails());
        // Console.WriteLine(lectureEvent.GetFullDetails());
        // Console.WriteLine(lectureEvent.GetShortDescription("Lecture"));

        // // Create at least one Reception object, set its values, and call its methods
        // Reception receptionEvent = new Reception(attributes);
        // Console.WriteLine(receptionEvent.GetStandardDetails());
        // Console.WriteLine(receptionEvent.GetFullDetails());
        // Console.WriteLine(receptionEvent.GetShortDescription("Reception"));

        // // Create at least one OutdoorGathering object, set its values, and call its methods
        // OutdoorGathering outdoorEvent = new OutdoorGathering(attributes);
        // Console.WriteLine(outdoorEvent.GetStandardDetails());
        // Console.WriteLine(outdoorEvent.GetFullDetails());
        // Console.WriteLine(outdoorEvent.GetShortDescription("Outdoor Gathering"));
    }
}